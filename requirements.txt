# Fundfolio — Streamlit Cloud requirements
# Core
streamlit>=1.35.0
numpy>=1.26.0
pandas>=2.2.0
matplotlib>=3.9.0
requests>=2.31.0

# Finance data
yfinance>=0.2.40

# PDF processing
pdfplumber>=0.11.0

# Charts (optional but loaded at startup)
plotly>=5.22.0

# Database (optional — only needed if you connect a DB)
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9
pymongo>=4.7.0

# --- NOT included (require system packages, add manually if needed) ---
# pytesseract       # needs tesseract-ocr system package
# pdf2image         # needs poppler system package
# vectorbt          # heavy dependency, install only if using backtesting
